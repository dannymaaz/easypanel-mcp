# Core dependencies
httpx>=0.25.0
python-dotenv>=1.0.0

# MCP Server
aiohttp>=3.9.0

# Type hints
typing-extensions>=4.8.0

# Official Model Context Protocol (MCP) SDK
# v2 is the current stable SDK line and provides MCPServer plus the
# 2026-07-28 protocol while remaining compatible with earlier MCP clients.
mcp>=2.0.0,<3.0.0
